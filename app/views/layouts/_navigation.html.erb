<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
 <%= link_to "Team Stackoverflow", root_path, class: "navbar-brand"%>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">

    <ul class="navbar-nav mr-auto">
      <% if user_signed_in? %>
      <li class="nav-item">
        <%= link_to "Home",root_path, class: 'nav-link' %>
      </li>

      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Questions
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
          <%= link_to "View Questions",questions_path, class: 'dropdown-item' %>
          <%= link_to "Ask Question",new_question_path, class: 'dropdown-item' %>
        </div>
      </li>
      <% end %>
    </ul>

      <%= form_for(:search, url:search_path, method: :get, :html => {class:"form-inline my-2 my-lg-0"}) do |f|   %>
      <%= f.text_field :key,class:'form-control mr-sm-2 md-12',placeholder: 'Search', value: (@key if @key.present?) %>
      <%= f.submit "Search", class: "btn btn-outline-success my-2 my-sm-0" %>
    <% end %>

    <ul class="navbar-nav">
      <% if user_signed_in? %>
      <li class="nav-item">
        <%= link_to "Profile",user_path(current_user), class: 'nav-link' %>
      </li>

      <li class="nav-item">
        <%= link_to "Logout",logout_path, class: 'nav-link' %>
      </li>
      <% end %>
    </ul>

  </div>

</nav>
